apiVersion: apps/v1
kind: Deployment
metadata:
  name: goutham
spec:
  selector:
    matchLabels:
      app: goutham
  template:
    metadata:
      labels:
        app: goutham
    spec:
      containers:
      - name: goutham
        image: gcr.io/augmented-ward-329505/gcme
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
